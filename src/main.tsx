import React from 'react'
import ReactDOM from 'react-dom/client'
import JobSearchPage from './Pages/JobSearch.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <JobSearchPage />
  </React.StrictMode>,
)
